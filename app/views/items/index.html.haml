%h1 Listing items

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Price
      %th Image url
      %th Recommended
      %th
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= link_to item.name, item
        %td= item.description
        %td= "#{item.price}円"
        %td= item.image_url
        %td= "おすすめ" if item.recommended?
        %td= link_to 'Show', item


