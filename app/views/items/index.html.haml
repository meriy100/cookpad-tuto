%h1 Listing items

%table
  %thead
    %tr
      %th
      %th Name
      %th Description
      %th Price
      %th Recommended
      %th
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= image_tag item.image_url, style: "width: 50px; height: 50px"
        %td= link_to item.name, item
        %td= item.description
        %td= "#{item.price}円"
        %td= "おすすめ" if item.recommended?
        %td= link_to 'Show', item


